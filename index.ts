/**
 * ClawSec 2.0 — OpenClaw Plugin Entry
 *
 * Re-exports the plugin for openclaw plugins install ./Clawsec2.0
 * or when the repo root is used as the plugin directory.
 */
module.exports = require("./src/coordinator");
