{
  "name": "clawsec-dashboard",
  "version": "2.0.0",
  "description": "ClawSec 2.0 — Security Operations Center Dashboard",
  "private": true,
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 8081",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 8081"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.0",
    "vite": "^5.0.0"
  }
}
